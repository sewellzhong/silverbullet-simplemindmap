{
  "name": "silverbullet-simplemindmap",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun run sync-assets && plug-compile simplemindmap.plug.yaml",
    "check": "biome check . && tsc --noEmit",
    "test": "bun test",
    "sync-assets": "bun run scripts/sync-assets.ts",
    "verify": "bun run check && bun run test && bun run build"
  },
  "dependencies": {
    "@silverbulletmd/silverbullet": "2.9.0",
    "lz-string": "1.5.0",
    "simple-mind-map": "0.14.0-fix.2",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.8",
    "@types/bun": "1.3.4",
    "typescript": "5.9.3"
  }
}
